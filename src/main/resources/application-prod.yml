server:
  servlet:
    encoding:
      charset: utf-8
      force: true
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blogdb
    username: root
    password: narf4661@@
  jpa:
    hibernate:
      ddl-auto: create 
    properties:
      hibernate:
        format_sql: true
  output:
    ansi:
      enabled: always
