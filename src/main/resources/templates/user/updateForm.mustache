{{> /layout/header}}
<div class="container p-5">
    <!--요청을 하면 localhost:8080/join POST로 요청됨
    username = 사용자입력값 & password = 사용자값 & email = 사용자입력값-->
    <div class="card">
        <div class="card-header"><b>회원정보를 수정해주세요</b></div>
        <div class="card-body">
            <form action="/updateForm" method="post" enctype="application/x-www-form-urlencoded">
                <!--method를 쓰지 않으면, 디폴트값이 GET이다.-->
                <div class="mb-3">
                    <input type="text" class="form-control" placeholder="Enter username" name="username" disabled>
                </div>
                <div class="mb-3">
                    <input type="password" class="form-control" placeholder="Enter password" name="password">
                </div>
                <div class="mb-3">
                    <input type="text" class="form-control" placeholder="Enter email" name="email" disabled>
                </div>
                <button type="submit" class="btn btn-primary form-control">회원수정완료</button>
                <!--submit : form tag 안에 존재해서, form 속 내용을 한 번에 등록시켜준다.-->
            </form>
        </div>
    </div>
</div>
{{> /layout/footer}}